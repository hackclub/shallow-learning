{
  "name": "website",
  "version": "0.1.0",
  "private": true,
  "description": "Simple React + Express website served on port 3000",
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon --ext js,jsx,ts,tsx,html --watch server --watch src --watch index.html --exec \"node server/index.js\"",
    "build": "vite build",
    "start": "node server/ensureDist.js && node server/index.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "nodemon": "^3.0.1",
    "vite": "^5.4.0"
  }
}
